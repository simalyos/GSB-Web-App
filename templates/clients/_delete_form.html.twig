<form method="post" action="{{ path('app_clients_delete', {'id_client': client.id_client}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.id_client) }}">
    <button class="btn">Delete</button>
</form>
